spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/iot_server?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: 123456
  mail:
    host: smtp.163.com
    username: 18589998412@163.com
    password: BVMNYDZNBLRIOCOI
    properties.mail.display.sendmail: 验证码
    properties.mail.display.sendname: Motb Account Register

# mybatis配置
mybatis:
  mapper-locations: classpath:mybatis/*.xml
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    root: info
    com.mt.base.dao: debug
  file:
    max-size: 20MB
    path: application-log

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka/
  instance:
    instance-id: ${spring.cloud.client.ip-address}:${spring.application.name}:${server.port}
    prefer-ip-address: true
